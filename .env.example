# 服务器配置
PORT=3000
NODE_ENV=development

# 日志级别
LOG_LEVEL=debug

# 说明：巨量引擎的Access-Token不再通过环境变量配置
# 而是通过API接口 POST /api/ad-accounts/{account_id}/token 设置

# MongoDB设置（如果需要）
# MONGODB_URI=mongodb://localhost:27017/mcp 